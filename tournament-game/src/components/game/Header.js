import React from 'react';
import {RoundInfo} from '../game';

function Header(props) {
    return (
        <div className='row'>
            <RoundInfo />
        </div>
    )
}

export default Header;